import Compound from "../compound";
import { Controller } from "./core"

export class Comp extends Controller {
  constructor(protocol: Compound) {
    super(protocol)
  }

  // all methods from comp
}
