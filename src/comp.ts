import Compound from "./Compound";

export class Comp {
  // I think this will be in another class and which is going to be the parent controller and comp
  // and governor alpha will extends from this class
  private _protocol: Compound;
  constructor(protocol: Compound) {
    this._protocol = protocol;
  }

  // all methods from comp
}
